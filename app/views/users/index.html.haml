
%h1 All users
 
%ul.ly-all-users
  = @users.each do |user| 
    = link_to user do
      %li.mod-user-profile
        = image_tag user.image.url
        = link_to user.username, user
        = user.simpleintroduce